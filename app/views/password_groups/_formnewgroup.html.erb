<%= form_for @password_group, remote: true do |f| %>
    <%= render 'shared/error_messages', object: f.object %>

    <%= f.label :name, label: "Group name" %>
    <%= f.text_field :name, class: "form-control" %>

    <br />
    <%= f.submit class: "btn btn-primary"  %>

<% end %>
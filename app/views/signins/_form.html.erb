<%= form_for @phone_number, url: signins_path, remote: true do |f| %>
    <%= render 'shared/error_messages', object: f.object %>

    <%= f.label :number %>
    <%= f.text_field :number, class: "form-control" %>

    <%= f.submit class: "btn btn-success" %>

<% end %>